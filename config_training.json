{
  "_comment": "Training configuration for h2_dispersion_gp.py. Set 'trainer' to 'exact' or 'approximate_additive' and fill the relevant fields.",
  "data": {
    "csv_path": "data/unified_preprocessed.csv"
  },
  "training": {
    "trainer": "approximate_additive",
    "split_ratio": 0.2,
    "n_inducing": 6000,
    "k": 16,
    "training_batch_size": 1024,
    "model_type": "VNNGP",
    "likelihood_type": "beta",
    "n_epochs": 300,
    "learning_rate": 0.008,
    "device": "cuda:0",
    "model_path": "models/approximate_indvAdditive_vnngp_rbf_k16_beta_300_lr8e-3.pth",
    "trained_model": null,
    "val_every_n_epochs": 10,
    "early_stopping_patience": 50,
    "mass_flow_lengthscale_min": 0.1,
    "use_source_mean": false
  }
}
