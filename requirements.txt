# Core numerical / ML stack
# Install CPU versions with: pip install -r requirements.txt
# For GPU training, install the CUDA-compatible builds of torch and gpytorch manually.

torch>=2.0.0
gpytorch>=1.11
pandas>=2.0.0
numpy>=1.24.0
scipy>=1.10.0
scikit-learn>=1.3.0

# Visualization and utilities
matplotlib>=3.7.0
tqdm>=4.65.0

# Jupyter notebooks for analysis
jupyter>=1.0.0
